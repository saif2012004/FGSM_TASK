# Requirements for AWS Lambda deployment
# Note: PyTorch for Lambda needs to be carefully managed due to size limits

fastapi==0.104.1
mangum==0.17.0
python-multipart==0.0.6
pydantic==2.5.0
pillow==10.0.1
numpy==1.24.3

# For Lambda, use a smaller PyTorch build
# torch==2.1.0+cpu --find-links https://download.pytorch.org/whl/torch_stable.html
# torchvision==0.16.0+cpu --find-links https://download.pytorch.org/whl/torch_stable.html

# Alternative: Use lighter ML libraries for Lambda
# scikit-learn==1.3.0
# tensorflow-cpu==2.13.0
